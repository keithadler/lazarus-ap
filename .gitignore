# Build output
/target

# Cargo.lock is omitted: this is a library-and-tools repo, not a
# published binary, and the toolchain is pinned by rust-toolchain.
Cargo.lock

# Reference scans are large and are linked from docs/SOURCES.md instead
*.pdf

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Editors and OS
.DS_Store
*.swp
.idea/
.vscode/
